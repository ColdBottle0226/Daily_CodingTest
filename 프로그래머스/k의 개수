function solution(i, j, k) {
    var answer = 0;
    let str = ""
    let strArr = []
    for(let x=i; x<=j; x++){
        str += x
    }
    strArr = [...str]
    answer = strArr.filter(item => item == k).length
    return answer;
}
