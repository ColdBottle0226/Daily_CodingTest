function solution(my_string, n) {
    var answer = '';
    let strArr = my_string.split("")
    
    let chgArr = strArr.map((item) => {
        return item.repeat(n)
    })
    
    answer = chgArr.join("")
    return answer;
}
