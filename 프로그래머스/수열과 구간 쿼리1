function solution(arr, queries) {
    var answer = [];
    for(let item of queries){
        for(let i=item[0]; i<=item[1];i++){
            arr[i] = arr[i] + 1
        }
    }
    
    return arr;
}
