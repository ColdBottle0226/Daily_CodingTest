### 1)
function solution(num_list, n) {
    var answer = [];
    answer = num_list.slice(0,n)
    return answer;
}

### 2)
function solution(num_list, n) {
    var answer = num_list.filter((item,index)=>index<n);
    return answer;
}
