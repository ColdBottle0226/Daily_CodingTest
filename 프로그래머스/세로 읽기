function solution(my_string, m, c) {
    var answer = '';
    let strArr = [...my_string]
    const startIdx = c-1
    for(let i=startIdx; i< my_string.length; i+=m){
        answer += (strArr[i])
    }
  
    return answer;
}
