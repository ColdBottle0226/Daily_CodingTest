### 1)
function solution(num_list) {
    var answer = 0;
    let sum = 0
    let mul = 1
    
    for(num of num_list){
        mul *= num
        sum += num
    }
    answer = (mul > sum * sum) ? 0 : 1
    return answer;
}
