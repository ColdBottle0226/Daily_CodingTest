function solution(numbers, k) {
    var answer = 0;
    let throwNum = 0;
    let peopleCnt = numbers.length
    
    throwNum = ((k-1) * 2) % numbers.length 
    answer = numbers[throwNum]
    return answer;
}
